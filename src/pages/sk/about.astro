---
import Base from '~/layouts/Base.astro';
import AboutPage from '~/components/AboutPage.astro';
import { useT } from '~/i18n/utils';

const lang = 'sk';
const t = useT(lang);
const path = '/sk/about';
---

<Base title={t('seo.about.title')} description={t('seo.about.description')} lang={lang} path={path}>
  <AboutPage lang={lang} />
</Base>
