# What I need help with now

1. **Rebrand/polish pobocek.dev** — implement the new dark design, remove blog, tighten hero with CTA, clean project layout
2. **Client acquisition strategy** — plan to start landing freelance web dev clients through the site
3. **Eviro development** — continue building the SaaS product in parallel

# Git workflow rules

- **NEVER commit or push directly to `main`**
- Always create a feature branch for changes (e.g. `feat/hero-mobile-layout`)
- After making changes, show a summary of what was changed and **wait for explicit confirmation** before committing
- After committing, **wait for explicit confirmation** before pushing to origin
- Ask: "Ready to commit?" and then "Ready to push?"
